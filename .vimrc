" Highlight searches
set hlsearch

" Default colour scheme
colorscheme murphy

" Enable Syntax highlighting
syntax on

" Line numbers on
set number

" Visual bell
set visualbell

" Set the tabwidth to 4 
set tabstop=4
set softtabstop=4

" I decided against automatically converting tabs to spaces as there are 
" occasions when tabs are required but if required, enable:
"set expandtab

" Set autoindent 
set autoindent

" Highlight search matches
set hlsearch
" Search as characters are entered
set incsearch
